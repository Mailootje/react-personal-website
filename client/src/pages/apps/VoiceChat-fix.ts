// Find this line at around line 178
              videoContainerRef.current.appendChild(videoElement);
              if (peerConnection) {
                peerConnection.videoElement = videoElement;
              }

// Find this line at around line 261
              videoContainerRef.current.appendChild(videoElement);
              if (peerConnection) {
                peerConnection.videoElement = videoElement;
              }
